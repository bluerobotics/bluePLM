/**
 * TeamDialogs - Self-contained team-related dialogs
 * 
 * NOTE: This component has been refactored. Team dialogs are now rendered
 * inline within TeamsTab. This component is kept for backward compatibility
 * but will render nothing if used standalone.
 * 
 * For new code, dialogs are managed by TeamsTab directly using hooks.
 * 
 * @deprecated Dialogs are now rendered inline in TeamsTab
 */

export function TeamDialogs() {
  // Dialogs are now rendered inline in TeamsTab using hooks directly.
  // This component is kept for backward compatibility with parent imports.
  return null
}
