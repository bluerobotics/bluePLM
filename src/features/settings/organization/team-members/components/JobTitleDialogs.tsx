/**
 * JobTitleDialogs - Self-contained job title dialogs
 * 
 * NOTE: This component has been refactored. Job title dialogs are now
 * rendered inline within TitlesTab. This component is kept for backward
 * compatibility but will render nothing if used standalone.
 * 
 * For new code, dialogs are managed by TitlesTab directly using hooks.
 * 
 * @deprecated Dialogs are now rendered inline in TitlesTab
 */

export function JobTitleDialogs() {
  // Dialogs are now rendered inline in TitlesTab using hooks directly.
  // This component is kept for backward compatibility with parent imports.
  return null
}
